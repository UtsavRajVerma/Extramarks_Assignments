class MyCalculator implements AdvancedArithmetic{
        int count=0;
        @Override
        public int divisor_sum(int n) {
            for(int i=1;i<=n;i++) {
                if(n%i==0) count+=i;
            }
            return count;
        }
        
    }
